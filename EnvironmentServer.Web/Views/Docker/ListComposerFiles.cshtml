@model IEnumerable<EnvironmentServer.DAL.Models.DockerComposeFile>

@{
    ViewData["Title"] = "Docker - List of composer files";
}

<div class="accordion" id="files">
    @foreach (var f in @Model)
    {
        <div class="accordion-item">
            <div class="row">
                <div class="col-10">
                    <h2 class="accordion-header">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse">
                            @f.Name class="form-control" />
                        </button>
                    </h2>
                </div>
                <div class="col-2">
                    <button class="btn btn-primary" type="submit" style="height: calc(100% - 10px); width: calc(100% - 10px); margin: 5px;">Edit</button>
                </div>
            </div>
        </div>
    }
</div>