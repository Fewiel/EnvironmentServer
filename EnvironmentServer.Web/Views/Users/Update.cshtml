@model EnvironmentServer.DAL.Models.User

@{
    ViewData["Title"] = "Update";
}

<h4>Editing User: @Model.Username</h4>

<div class="d-flex align-items-center justify-content-center fullHeight">
    <form method="post" asp-action="Registration" style="width: 400px;">
        <input type="hidden" asp-for="ID" />
        <div class="mb-3">
            <label asp-for="Email" class="form-label">E-Mail</label>
            <input asp-for="Email" class="form-control" />
            <span asp-validation-for="Email"></span>
        </div>
        <div class="mb-3">
            <input class="form-check-input" type="checkbox" asp-for="IsAdmin">
            <label class="form-check-label" asp-for="IsAdmin">Admin</label>
        </div>
        <button type="submit" class="btn btn-primary">Sign up</button>
    </form>
</div>